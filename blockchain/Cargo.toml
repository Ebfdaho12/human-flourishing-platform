[workspace]
members = [
    "programs/found-token",
    "programs/governance",
]
resolver = "2"

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1

[profile.dev]
overflow-checks = true
